# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# Cloudflare Worker local dev — contains real Firebase service-account
# credentials/JWT secret when testing against a real project; must never be
# committed (see docs/MIGRATION.md §7).
worker/.dev.vars
worker/.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
